import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  message: {
    color: "#ff0043",
    fontWeight: "bold",
    fontSize: 18,
    textAlign: "center",
  },

  result: {
    color: "#ff0043",
    fontWeight: "bold",
    fontSize: 50,
    textAlign: "center",
  },
});

export default styles;
